meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "10.0"

build:
  image: mcr.microsoft.com/dotnet/sdk:10.0

run:
  image: mcr.microsoft.com/dotnet/aspnet:10.0
  buildFileName: MuseumHub.API
  containerPort: 8080
  persistenceMount: /data